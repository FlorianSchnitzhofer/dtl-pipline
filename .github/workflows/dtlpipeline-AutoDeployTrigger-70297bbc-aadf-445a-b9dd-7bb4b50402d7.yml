name: Trigger auto deployment for dtlpipeline

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/dtlpipeline-AutoDeployTrigger-70297bbc-aadf-445a-b9dd-7bb4b50402d7.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.DTLPIPELINE_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.DTLPIPELINE_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.DTLPIPELINE_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.DTLPIPELINE_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.DTLPIPELINE_REGISTRY_PASSWORD }}
          containerAppName: dtlpipeline
          resourceGroup: Binary-Research
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


