FROM python:3.11-slim
WORKDIR /app
COPY src/backend/requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY src/backend ./
ENV DATABASE_URL=mysql+pymysql://dtl:dtl@db:3306/dtl
ENV PYTHONPATH=/app
CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]
